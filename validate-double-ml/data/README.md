# Data

This project requires **no external data**. Every number in the article and in
`code/verify_double_ml.py` is produced by simulation from a fixed seed, so the
results are fully reproducible with only the packages in `requirements.txt`.

The `raw/` and `processed/` folders are kept for repository-structure
consistency and are intentionally empty.
