# Data

No external data. The verification demo generates its own simulated panels with a
**planted** treatment effect (true ATT = 1.0) and checks whether the estimator recovers it.
Everything is reproducible from `code/` with a fixed seed; no downloads required.
